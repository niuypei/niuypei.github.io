# Legacy publication sources

The publication metadata verification is retained in [publication-verification.json](publication-verification.json).

The original legacy HTML snapshots and raw extraction have been kept outside the repository because they include historical contact details and a complete CV link. They are not website assets and are not required to build the homepage.

Current public content is maintained in `content/profile.json` and `content/publications.json` at the repository root.
